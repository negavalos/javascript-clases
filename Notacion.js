//Notacion

// ; --> delimitar el final de una linea
const b = 4;
b + 1;

// .---> se ultiliza en los objetos para acceder a los atributos
// movil.anchura

// []-->liastas, arreglos, array
const ar = [1,2,3,5,];
console.log(ar[2]);
//()--->funciones
function suma(a, b) {
    //Se escribe la funcion
}
// {} --->llaves para obetos, funciones y estructuras de control

const movil = {
    anchura: 5,
    altura: 10
}

if (true) {
    const constante2 = "hola"
}